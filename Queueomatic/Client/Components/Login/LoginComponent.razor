@using Blazored.SessionStorage
@inject HttpClient _httpClient;
@inject NavigationManager _navigationManager;
@inject ISessionStorageService _sessionStorageService;

<LogoComponent/>
<AuthorizeView>
    <Authorized>
        <RedirectHome/>
    </Authorized>
</AuthorizeView>
<div class="component">
    <div class="form-container">
        <p class="title">Login</p>
        <EditForm class="form" Model="_loginDto" OnValidSubmit="Login">
            <div class="input-group">
                <label for="username">Username</label>
                <input type="text" name="username" placeholder="" id="input-field" @bind-value="_loginDto.Email">
            </div>
            <div class="input-group">
                <label for="password">Password</label>
                <input type="password" name="password" placeholder="" id="input-field" @bind-value="_loginDto.Password">
                <div class="forgot">
                    <a rel="noopener noreferrer" href="#">Forgot Password?</a>
                </div>
            </div>
            <button class="submit-button">
                <span>LOGIn</span>
            </button>
        </EditForm>
        <div class="line-container">
            <div class="line"></div>
        </div>
        <p class="signup">
            Don't have an account?
            <br/>
            <a rel="noopener noreferrer" href="signup" class="signup-a">Sign up</a>
        </p>
    </div>
</div>